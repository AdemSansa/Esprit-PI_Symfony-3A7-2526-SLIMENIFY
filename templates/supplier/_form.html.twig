{{ form_start(form, {'attr': {'class': 'auth-form'}}) }}
    {{ form_widget(form) }}
    <button class="auth-button" style="margin-top: 1.5rem; background: #627255; color: white; padding: 0.75rem 1.5rem; border: none; border-radius: 8px; cursor: pointer; font-weight: 500;">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
