{% extends 'layout/app_shell.html.twig' %}

{% block title %}New Quiz{% endblock %}

{% block app_content %}
    <h1 style="margin-top:0; font-size:1.75rem;">Create new Quiz</h1>

    {{ include('quiz/_form.html.twig', {'button_label': 'Create Quiz'}) }}

    <div style="margin-top: 1.5rem;">
        <a href="{{ path('app_quiz_index') }}" class="auth-link">← Back to list</a>
    </div>
{% endblock %}
