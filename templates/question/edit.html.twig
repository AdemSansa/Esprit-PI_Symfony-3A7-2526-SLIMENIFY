{% extends 'layout/app_shell.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block app_content %}
    <h1 style="margin-top:0; font-size:1.75rem;">Edit Question</h1>

    {{ include('question/_form.html.twig', {'button_label': 'Update Question'}) }}

    <div style="margin-top: 1.5rem; display: flex; gap: 1rem; align-items: center;">
        <a href="{{ path('app_question_index') }}" class="auth-link">← Back to list</a>
        
        {{ include('question/_delete_form.html.twig') }}
    </div>
{% endblock %}
