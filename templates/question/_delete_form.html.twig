<form method="post" action="{{ path('app_question_delete', {'id': question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="margin: 0; display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.id) }}">
    <button style="background: #ef4444; color: white; border: none; padding: 0.4rem 0.75rem; border-radius: 6px; cursor: pointer; font-size: 0.85rem; font-weight: 500;">Delete</button>
</form>
